# Agent Handoff Template

Use this template at the end of an agent session.

## 1. Issue
- Claimed bead: `<id>`
- Status: `closed` | `in_progress`

## 2. What Was Done
1. `<change 1>`
2. `<change 2>`
3. `<change 3>`

## 3. Files Changed
1. `<path>`
2. `<path>`
3. `<path>`

## 4. Validation Run
1. `<command>` -> `<pass/fail + key output>`
2. `<command>` -> `<pass/fail + key output>`

## 5. New Beads Created
1. `<id>` - `<title>` - `<why created>`
2. `<id>` - `<title>` - `<why created>`

## 6. Remaining Risks / Notes
1. `<risk or edge case>`
2. `<open question>`

## 7. Next Suggested Action
1. `<specific next step>`

## 8. Landing the Plane Check
1. `git pull --rebase` completed
2. `bd sync` completed
3. `git push` completed
4. `git status` confirms up to date with origin
